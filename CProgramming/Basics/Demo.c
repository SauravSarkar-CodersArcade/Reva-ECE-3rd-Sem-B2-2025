#include<stdio.h>
#include<stdbool.h>
int main(){
    // In C we don't have boolean directly, we use <stdbool.h>
    // %d -> format specifier
    // We can wrute %d or %i for intergers
    bool status = true; // 
    printf("%d\n", status);
    printf("%i\n", status);
    int _name = 20;
    int $data = 30;
    // Camel Naming Convention - camelCase - rateOfInterest - vars, functions
    // Pascal Naming COnvention - RateOfInterest - class, Constants
    int rateOfInterest = 20;
    return 0;
}